# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_ID=

# ETH (Alchemy — free tier is enough for personal use)
ALCHEMY_API_KEY=

# Solana RPC (public RPC works, or use a free-tier provider like Helius)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# NOTE: this bot is READ-ONLY. It never needs a private key.
# Only public wallet addresses go in wallets.json — never put a seed
# phrase or private key anywhere in this project.
